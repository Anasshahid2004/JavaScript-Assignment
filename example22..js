# Create an array with 4 elements.
my_array = [1, 2, 3, 4]

# Try to access the element at index 5, which is out of bounds.
print(my_array[5])

IndexError: list index out of range

# Create an array with 4 elements.
my_array = [1, 2, 3, 4]

# Check the length of the array.
if 5 < len(my_array):
  # Access the element at index 5.
  print(my_array[5])
else:
  # Print an error message if the index is out of bounds.
  print("Error: index out of bounds.")

  Error: index out of bounds.
